# ORM model package

SQLAlchemy model modules are added with their Alembic migration in the phase that owns each aggregate. The planned aggregates are users/auth, projects/files/sessions, execution, learner profile/hints/stuck score, interviews, problems/attempts, analytics, and gamification.
